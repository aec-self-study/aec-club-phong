version: 2
 
models:
  - name: customers
    description: '{{ doc("my_doc_block") }}'
    columns:
      - name: customer_id
        description: Primary key of the model
        tests:
          - unique
          - not_null
      - name: name
        tests:
          - accepted_values:
              values: ['Debra Castro', 'Phong Le', 'Scott Foreman', 'Emily Wasserman']